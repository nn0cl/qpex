# Doc audit: QPex architecture sync (2026-07-23)

Companion to ongoing Architecture Path work. Implementation remains **Hold**.

## Executive verdict

Core locks (Never Leave the State, universal `State<T>`, `measure`-only collapse,
`map`/`project`/`interfer`, no exceptions, Kotlin DX + packages) are **aligned
in the umbrella + recent ADRs**. Residual drift was mostly **stale examples**
still teaching `span` / keyword `system` / `trait` / §7=measure; P0 example
sync applied in this pass. Historical ADRs/research that say `observe` remain
as archives — fine if not copied into new normative text.

## Consistency matrix (locks)

| Lock | Canonical | Status |
|------|-----------|--------|
| Never Leave the State | positioning, axioms, language-spec | OK |
| Universal `State<T>` + Lit-Lift | ADR 0018, 0024, type-system §4 | OK |
| Terminal `measure` only | semantics §9, AST | OK (baseline §# fixed) |
| Mixture surface | `when` (ADR 0024); §Span math name | OK after P0 |
| Capsule surface | `class` / `interface System` | OK after P0 stdlib/abstraction |
| Stdlib | `map` / `project` / `interfer` (0021) | OK |
| No exceptions | ADR 0025, language-spec §1.3 | OK |
| Packages as $\mathcal{H}$ borders | ADR 0024, language-spec §2 | OK |
| Naming style | ADR 0023 | OK |
| Quantum opts | ADR 0022 | OK (design Hold) |
| Kernel PoC fixtures | `tests/fixtures/poc/` | OK (no span/observe) |

## ADR index 0013–0025

| ADR | Topic | Status |
|-----|-------|--------|
| 0013 | Language axioms | Accepted |
| 0014 | Discrete PMF MVP | Accepted |
| 0015 | Local-first / ports | Accepted (text may still say `observe`) |
| 0016 | PMF → amplitude lift | Accepted |
| 0017 | Surface vocab | Accepted; **mixture spelling → 0024** |
| 0018 | `State<T>` / lift | Accepted; strengthened by 0024 |
| 0019 | Generics / capsules | Accepted; **surface → 0024** |
| 0020 | map/given/fold | **Superseded naming** by 0021 |
| 0021 | map/project/interfer/System | Accepted |
| 0022 | Quantum-native opts | Accepted design; IR Hold |
| 0023 | Naming conventions | Accepted style; linter Hold |
| 0024 | Kotlin DX / packages / when / class | Accepted |
| 0025 | Failure as superposition | Accepted |

## P0 fixes applied this pass

- `qpex-stdlib-combinators.md` examples → `interface` / `class` / `fun`
- `qpex-abstraction-model.md` integration map + keyword narrative → ADR 0024
- `README.md` lexicon `span` → `when`
- `naming-conventions.md` wildcard wording
- `agent-sync-qpex-baseline.md` AST nodes, §9 measure, dates, ADR rows
- Formal semantics checklist / purity bullets → `when` surface
- Soft updates: ADR 0021, compiler-opts, research open-questions

## Remaining gaps (P1 / P2)

### P1 — still teachable as “current” if an agent skims wrong files

- Early ADRs **0013–0015** and `qpex-mvp-*-observe.md` filename / body still
  say `observe` (historical MVP name). Add one-line banners pointing to
  `measure` where missing.
- `docs/research/2026-07-22-prior-art-*.md` still contrasts on `observe`.
- `LISS-0001` issue text still says Discrete PMF + `observe`.
- Formal section **title** remains “§3. Span” (intentional math name) — OK if
  agents always see the ADR 0024 surface note (already present).

### P1 — open design tensions (not contradictions, but unresolved)

- `fun` vs `fn` alias duration; semicolon / `public static fun main` vs script
  Kernel style.
- `Result` / `Success` / `Error` **stdlib name lock** (pattern accepted; type
  name not ADR-locked).
- `project` $Z=0$ host UX vs “never crash” narrative.
- ALL_CAPS constants are still `State<T>` after lift — style vs type story
  needs a one-liner in naming guide (partially in language-spec).
- Package ↔ filesystem strictness; extension orphan rules.
- Interfer surface lambda `(acc, c) => acc + c` vs threaded `State<Acc>`
  signature (sugar vs law) — keep calling out in stdlib.

### P2 — hygiene

- Rename MVP spec file away from `observe` when convenient.
- Trace docs under `docs/collaboration/traces/` are historical — no need to
  rewrite.
- Canvas / prior-art “observe” wording.

## Sync health

**~8.5 / 10** after P0 pass. Safe to treat `qpex-language-spec.md` +
`agent-sync-qpex-baseline.md` as the agent entry; do not start from ADR 0013
alone without 0024–0025.

## Recommended next doc actions

1. Banner on ADR 0013–0015 + MVP observe spec: “surface keyword is `measure`”.
2. Short ADR or language-spec subsection locking `Result` carrier (optional).
3. One “migration cheat sheet” page: old → new spellings (span/system/fn/…).
4. Hold remains on harness / parser / typechecker / styler.


---

## Follow-up (same day) — Adjudicator P1 reply applied

Locks (ADR **0026**):

- `fun` only (`fn` abolished)
- `Result<T, E>`
- `project` $Z=0$ → `Vacuum` (not exception; not identity)
- `package` required for class/interface/top-level fun units

Delivered:

- Historical banners on ADR 0013–0015 + MVP observe spec
- `docs/collaboration/spelling-cheat-sheet.md`

**Revised sync health: ~9.5 / 10.** Remaining: Vacuum encoding mini-spec,
default `E`, package↔path strictness. Hold-unseal judgment is with Adjudicator.
