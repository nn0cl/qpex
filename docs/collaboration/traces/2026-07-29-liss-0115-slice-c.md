# Trace: LISS-0115 Slice C

| Field | Value |
|---|---|
| Date | 2026-07-29 |
| Issue | LISS-0115 |
| Scope | Slice C — Equation/Unit consumption in lowering |
| Phase | Phase 2 Green; review pending |
| Branch | `codex/liss-0115-slice-c` |
| Base | `origin/main` at `db3a3f3` |
| Implementation | `fa87858` |

## Design check

The lowering API consumes an immutable `HirModule`, the optional typed unit,
and an explicit ordered sequence of LISS-0116 `EquationNode` values. It emits
an immutable `PhysicsModule` without changing `physics_ir.py`,
`physics_equation.py`, `pipeline.py`, or the evaluator.

Included: the LISS-0115 Issue, LISS-0115 plan, LISS-0116 DTO module and tests,
the frozen Physics IR boundary, and the A–B lowering tests. Omitted: pipeline
wiring, golden loading, numerical execution, provider adapters, and Equation
DTO definition changes.

## Evidence

- Equation order, coefficient/unit retention, and source provenance pass.
- Invalid nested Equation contracts return named Equation diagnostics.
- Generic equation payloads are rejected instead of silently coerced.
- Direct Slice C, Physics IR A–D, Equation A–B, `py_compile`, and
  `git diff --check` pass.

## Review boundary

Implementation permission was scoped to LISS-0115 Slice C. The result is
awaiting Adjudicator review. Slice D `compile_source` wiring remains outside
this approval.
