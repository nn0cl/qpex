# AI work trace — WP-0082 LISS-0235 Kernel RngPort

| Field | Value |
|---|---|
| Date | 2026-08-02 |
| Branch | `batch/wp-0082-kernel-rng-port` |
| Issue | LISS-0235 |
| Ship ADR | 0170 |

## Change

- New `compiler/staqex/rng_port.py`: `RngPort` Protocol + `StdlibRngAdapter`.
- `Evaluator` takes `rng_port=` (or wraps `rng=` / `seed=` via adapter);
  no `random.Random(...)` construction inside `evaluator.py`.
- Host `HostRngPort` left untouched.
- Red suite `tests/test_liss0235_kernel_rng_port_red.py` (seed pin B01 → `42\n`).

## Verification

`.venv/bin/pytest tests/` → 1073 passed / 0 failed.
