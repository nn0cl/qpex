# Trace: LISS-0076 Slice A Phase 1 Red → Phase 3

| Field | Value |
|---|---|
| Date | 2026-07-29 |
| Issue | LISS-0076 |
| Slice | A — Theory body vs Execution symbol |
| Phase | phase-1-red → phase-2-green → phase-3-refactor |
| Branch | `feature/liss-0076-slice-a` |
| Approval | Adjudicator plan + Slice A authorization (“承認”) |

## [DESIGN CHECK]

- Scope: Theory `body_declarations` OpVar that names an Execution-bound
  symbol (not lexeme `shots`/`backend`/`retry`/`Host`) →
  `PHASE_TYPE_VISIBILITY_ERROR`; compile hard-fails.
- Specs: LISS-0076 Gherkin Slice A; keep existing
  `PHASE_SCOPE_DEPENDENCY_ERROR` lexeme path.
- Components: `scientific_scopes.check_theory_execution_body_visibility`
  via `resolve_scientific_scopes`.
- Omitted: Slice B–E matrix, imports, generics, methods; 0077/0081.
- Routing: Feature Path / Cursor Agent
- Verification: `python3 tests/test_body_phase_slice_a_red.py`;
  `python3 tests/test_scientific_scopes_red.py`

## Delivered

- `tests/test_body_phase_slice_a_red.py`
- `compiler/staqex/scientific_scopes.py` — body visibility walk
- Issue / open-work-register / WP-0025 Slice A status

## Verification

- Red: assertion missing `PHASE_TYPE_VISIBILITY_ERROR` before Green
- Green: Slice A + scientific scopes suite PASS
- Refactor: helpers kept small; no behavior change

### 変更の要約 (PR Summary)
- **何を目的として何を変更したか**: Theory 式が Execution 記号を参照したとき
  位相診断 `PHASE_TYPE_VISIBILITY_ERROR` を出す（lexeme 経路は維持）。

### 残存リスク・検証の溝 (Verification Gap)
- Execution `symbols` 収集はパーサの `ident =` ヒューリスティック依存。
- Experiment/Workflow/Report 行列・import/generic は B–D。
- 同名 shadow / theory 内バインダ変数は未カバー（Slice A 外）。

## Next safe action

Slice B authorized and completed — see
[`2026-07-29-liss-0076-slice-b.md`](2026-07-29-liss-0076-slice-b.md).
