# Trace: LISS-0118 Slice A Phase 1–3

| Field | Value |
|---|---|
| Date | 2026-07-29 |
| Issue | LISS-0118 |
| Slice | A — transitive call taint |
| Phase | phase-1-red → phase-2-green → phase-3-refactor |
| Branch | `feature/liss-0118-slice-a` |
| Approval | Adjudicator plan + Slice A (“承認”) |

## [DESIGN CHECK]

- Scope: Theory calling `mid()` that only calls `leak()` (which names an
  Execution symbol) must yield `PHASE_TYPE_VISIBILITY_ERROR`.
- Gap: one-hop taint only; `return leak()` is `OpCall`, not `Call`.
- Verification: `tests/test_body_phase_slice_a_0118_red.py` + 0076 suite

## Delivered

- Transitive fixpoint in `_execution_tainted_callables`
- `OpCall.name` as call target
- Issue / register Slice A status

### 変更の要約 (PR Summary)
- **何を目的として何を変更したか**: Execution 記号への推移的 fn 呼び出しを
  Theory から拒否する。

### 残存リスク・検証の溝 (Verification Gap)
- 再帰相互呼び出しは fixpoint でカバー; 無限名集合は想定外。
- Report 行列・短名 closeout は B–C。

## Next safe action

Slice B authorized and completed — see
[`2026-07-29-liss-0118-slice-b.md`](2026-07-29-liss-0118-slice-b.md).
