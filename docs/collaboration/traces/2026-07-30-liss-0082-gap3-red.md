# LISS-0082 gap 3 — Phase 1 Red

- Date: 2026-07-30
- Branch: `feature/liss-0082-slice-b-red`
- Operating path: Feature Path
- Issue: LISS-0082
- Scope/phase: Slice B follow-up 2 (gap 3) / Phase 1 Red
- Approval: Adjudicator message `承認` after scoped architecture approval of
  ADR 0108 §1a, interpreted only as the separately gated gap 3 Red
- Implementation permission: **none**
- Post-review required: Adjudicator review before Phase 2 Green

## Acceptance boundary

One `value_id` identifies one immutable whole-Joint-state generation. Both
`PureJointStateValue` and `DensityJointStateValue`:

- construct without a separate `generation` argument;
- expose no `.generation` attribute;
- reject a `generation=` keyword.

The previously reviewed Slice B tests are mechanically migrated to the approved
constructor shape. Existing assertions, scenarios, diagnostic expectations,
and generation-semantic terminology remain intact.

## Files

- Added `tests/test_quantum_semantic_ir_slice_b_gap3_red.py`.
- Removed only constructor `generation=` entries from
  `tests/test_quantum_semantic_ir_slice_b_red.py` and
  `tests/test_quantum_semantic_ir_slice_b_followup_red.py`.
- Renamed the original Slice B helper's value-ID ordinal parameter from
  `generation` to `value_ordinal`.
- No file under `compiler/` changed.

## Red evidence

`python3 tests/test_quantum_semantic_ir_slice_b_gap3_red.py` exits 1:

- 0 passed / 4 failed;
- pure and density construction without the field fail because the shipped
  constructor still requires `generation`;
- pure and density construction with `generation=` succeeds, while the
  approved contract requires rejection.

The two mechanically migrated Slice B suites also fail at construction because
the shipped DTO still requires the removed field. This is the expected API
subtraction boundary, not an assertion workaround. Slice A remains green.

## Deterministic checks

- all three changed test files pass `py_compile`;
- `tests/test_quantum_semantic_ir_slice_a_red.py` passes;
- `git diff --check` is clean;
- `git status --short compiler/` is empty.

## Stop condition

Stop after Red evidence. Do not edit `compiler/`, weaken the reviewed
assertions, begin Green/Refactor, start Slice C, open a PR, or push without
separate approval.
