# Trace: LISS-0076 Slice B Phase 1 Red → Phase 3

| Field | Value |
|---|---|
| Date | 2026-07-29 |
| Issue | LISS-0076 |
| Slice | B — Experiment/Workflow ↔ Execution matrix |
| Phase | phase-1-red → phase-2-green → phase-3-refactor |
| Branch | `feature/liss-0076-slice-a` |
| Approval | Adjudicator Slice B authorization (“承認”) |

## [DESIGN CHECK]

- Scope: Experiment/Workflow `body_declarations` naming Execution-bound
  symbols → `PHASE_TYPE_VISIBILITY_ERROR`; Execution→Theory downward OK;
  Experiment→Theory OK. Lexeme path unchanged.
- Specs: LISS-0076 Slice B; `staqex-scientific-scopes.md` dependency
  direction (`execution -> workflow -> experiment -> theory`).
- Components: generalize `check_execution_symbol_body_visibility` to
  Theory/Experiment/Workflow + `Var`/`OpVar` walk.
- Omitted: import/module (C), generics/methods (D), catalog (E); Report.
- Ambiguity noted: Workflow may later declare phase-local ShotCount types
  without naming Execution-scope symbols — not in this slice.
- Verification: `tests/test_body_phase_slice_{a,b}_red.py`;
  `tests/test_scientific_scopes_red.py`

## Delivered

- `tests/test_body_phase_slice_b_red.py`
- `compiler/staqex/scientific_scopes.py` — matrix + `Var` refs
- Issue / register / WP-0025 Slice B status

## Verification

- Red: missing `PHASE_TYPE_VISIBILITY_ERROR` on Experiment/Workflow fixtures
- Green: A+B + scientific scopes PASS
- Refactor: single checker name; alias retained

### 変更の要約 (PR Summary)
- **何を目的として何を変更したか**: Experiment/Workflow からも Execution
  記号参照を位相診断で拒否し、下向き（Execution→Theory）は許可した。

### 残存リスク・検証の溝 (Verification Gap)
- `execution` の `symbols` はパーサ `ident =` ヒューリスティック。
- Workflow の正規フィールド面（`parameter`/`until`）内の名前は未走査。
- import / generic / method は C–D。

## Next safe action

Slice C authorized and completed — see
[`2026-07-29-liss-0076-slice-c.md`](2026-07-29-liss-0076-slice-c.md).
