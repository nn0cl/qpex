# Trace: LISS-0076 Slice C Phase 1 Red → Phase 3

| Field | Value |
|---|---|
| Date | 2026-07-29 |
| Issue | LISS-0076 |
| Slice | C — import / module-boundary phase |
| Phase | phase-1-red → phase-2-green → phase-3-refactor |
| Branch | `feature/liss-0076-slice-a` |
| Approval | Adjudicator Slice C authorization (“承認”) |

## [DESIGN CHECK]

- Scope: `compile_path` import graph must apply body-level phase visibility
  across modules. Imported Execution symbols invisible to entry Theory;
  imported Theory usable by entry Experiment.
- Gap before Green: `merge_modules` dropped `ScientificScopeDecl`, so
  cross-file Execution symbols never entered the checker.
- Components: `compiler/staqex/modules.py` always merges scientific scopes.
- Omitted: Slice D generics/methods; Slice E catalog; fixing pre-existing
  A02 `BinOp.left` HIR crash (unrelated).
- Verification: `tests/test_body_phase_slice_{a,b,c}_red.py`

## Delivered

- `tests/test_body_phase_slice_c_red.py`
- `modules.merge_modules` — always merge `ScientificScopeDecl`
- Issue / register / WP-0025 Slice C status

## Verification

- Red: missing `PHASE_TYPE_VISIBILITY_ERROR` / broken Theory import
- Green: A–C PASS; scientific scopes PASS
- Note: `test_liss0107` official A02 fails with pre-existing
  `BinOp.left` AttributeError (reproduced without Slice C merge)

### 変更の要約 (PR Summary)
- **何を目的として何を変更したか**: import 連結後も科学スコープをマージし、
  モジュール境界で位相可視性をenforceする。

### 残存リスク・検証の溝 (Verification Gap)
- スコープは `pub` 無しでもグラフ全体にマージ（位相検査優先）。
- 同名スコープ衝突ポリシーは未規定。
- generic/method 漏れは D。

## Next safe action

Slice D authorized and completed — see
[`2026-07-29-liss-0076-slice-d.md`](2026-07-29-liss-0076-slice-d.md).
