# Trace: LISS-0076 Slice D Phase 1 Red → Phase 3

| Field | Value |
|---|---|
| Date | 2026-07-29 |
| Issue | LISS-0076 |
| Slice | D — generic call / method leak paths |
| Phase | phase-1-red → phase-2-green → phase-3-refactor |
| Branch | `feature/liss-0076-slice-a` |
| Approval | Adjudicator Slice D authorization (“承認”) |

## [DESIGN CHECK]

- Scope: Theory (blocked phases) must not (1) pass Execution symbols through
  Call/BinOp/Attr trees, nor (2) call fn/methods whose bodies name Execution
  symbols. main may still call tainted fns.
- Components: extend `_iter_name_refs`; add callable taint + `_iter_call_targets`;
  pipeline passes `unit_decls` into `resolve_scientific_scopes`.
- Omitted: Slice E catalog; transitive taint through helpers; Report phase.
- Verification: `tests/test_body_phase_slice_{a,b,c,d}_red.py`

## Delivered

- `tests/test_body_phase_slice_d_red.py`
- `scientific_scopes.py` + `pipeline.py` wiring
- Issue / register / WP-0025 Slice D status

## Verification

- Red → Green: Slice D assertions PASS; A–C + scientific scopes PASS

### 変更の要約 (PR Summary)
- **何を目的として何を変更したか**: Theory からの Call 引数リークと、
  Execution 記号を使う fn/method 呼び出しを位相診断で拒否した。

### 残存リスク・検証の溝 (Verification Gap)
- メソッド汚染は短名 `k` と `S.k` の両方でマッチ（同名衝突時に過検知しうる）。
- ヘルパー経由の推移的汚染は未実装。
- Slice E で catalog / Gherkin を閉じる。

## Next safe action

Slice E closeout complete — Issue **complete**. See
[`2026-07-29-liss-0076-slice-e.md`](2026-07-29-liss-0076-slice-e.md).
