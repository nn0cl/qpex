// Topology domain — enum / struct (blackboard classifications + geometry)

package com.qpex.examples.topological_physics.domain

namespace Topology {
    pub enum BoundaryCondition {
        Periodic,
        Open
    }

    pub struct ChainLattice {
        val num_cells: Int
        val bc: BoundaryCondition
    }
}
