// CH-comms — Noisy order channel (Lindblad toy / C-box narrative). Locked seat.
// Full CPTP city model is not claimed.

package com.staqex.examples.showcase.s01_disaster.comms_lane

pub fn main() -> Unit {
    Operator H = X
    DensityState<Qubit> rho = DensityState(
        RawMatrix([
            [1.0, 0.0],
            [0.0, 0.0]
        ])
    )
    DensityState<Qubit> observed = lindblad(
        rho,
        H,
        JumpSet([RawMatrix([
            [0.0, 1.0],
            [0.0, 0.0]
        ])]),
        0.1
    )
    state rho = vacuum
    measure observed
}
