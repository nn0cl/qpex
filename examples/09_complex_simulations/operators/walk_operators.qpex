// Walk operators — exported Operator + one DTQW step (ADR 0054)

package com.qpex.examples.complex_simulations.operators

pub fn build_coin_operator() -> Operator {
    Operator Coin = (X + Z) * inv_sqrt2
    return Coin
}

pub fn step_quantum_walk(operator: Operator, c: State<Qubit>, x: State<Position>) -> State<(Qubit, Position)> {
    State<Qubit> c = apply(operator, c)
    State<Position> x = walk_shift(c, x)
    return c *|* x
}
