# B04 — `evolve` not loops

Teaches axiom 4: unitary Schrödinger steps use `evolve … under H for t`, not
`while` / `for` (rejected).

Legacy source: `examples/02_quantum_basics/ket_evolve_expect.sqx`.
