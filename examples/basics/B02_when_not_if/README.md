# B02 — `when` not `if`

Teaches axiom 3: use `when` for controlled mixture, not classical `if` (rejected
as `FORBIDDEN_KEYWORD`).

Legacy source: `examples/02_quantum_basics/double_slit.qpex` (simplified).
