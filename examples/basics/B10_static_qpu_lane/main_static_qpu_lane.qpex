// B10 — static register elaboration; `q` is an opaque wire handle.

package com.qpex.examples.basics.static_qpu_lane

pub fn main() -> Unit {
    QubitRegister<3> reg = system()
    forEach q in reg {
        apply(H, q)
    }
    State<Int> observed = coin()
    measure observed
}
