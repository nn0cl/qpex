# B01 — Never Leave the State

Teaches QPex axiom 1 and 5: every mid-program value is a `State<T>`; sampling
happens only at terminal `measure`.

```bash
python3 -m compiler.qpex run examples/basics/B01_never_leave_the_state/never_leave_the_state.qpex --seed 0
```
