# B12 — Open systems

Teaches `DensityState`, `lindblad`, and explicit `JumpSet` inputs (ADR 0057).

```bash
python3 -m compiler.staqex run examples/basics/B12_open_systems/main_open_systems.sqx --seed 0
```

Legacy source: Lindblad slice from `examples/16_quantum_observatory/main_observatory.sqx`.
