# B03 — Failure as world-line

Teaches axiom 6 / ADR 0025: fallible arithmetic encodes `Err` carriers in the
joint store instead of `throw` / `try` / `catch`.

Inspect before `measure` to see both success and `DivByZero` world-lines coexist.
