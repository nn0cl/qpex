# B06 — Type-First dimensions

Teaches dimensional quantities (`Length`, `Momentum`, `Delta<Time>`) and
correlated `evolve` on phase-space tuples.

Legacy sources: `examples/01_classical_mechanics/phase_space.qpex`,
`examples/05_harmonic_oscillator/classical_oscillator.qpex`.
