# B07 — Structure and visibility

Teaches `namespace`, `enum`, `struct`, `class`, `fn init`, `pub`, and class-private
`_` fields (module-private by default).

Legacy source: distilled from `examples/10_topological_physics/domain/`.
