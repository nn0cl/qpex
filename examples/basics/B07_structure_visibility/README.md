# B07 — Structure and visibility

Teaches `namespace`, `enum`, `struct`, `class`, `fn init`, `pub`, and
class-private `_` fields (module-private by default).

**WP-0088 / LISS-0262:** geometry uses `struct`/`enum`; `class` is a small
**physical system** that builds a Hamiltonian — not a mutable enterprise
Tracker. See [surface modernization north star](../../../docs/architecture/surface-modernization-north-star.md).

Legacy source: distilled from `examples/10_topological_physics/domain/`.
