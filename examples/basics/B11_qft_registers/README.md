# B11 — QFT on a static register

Teaches `qft` / `iqft` over `QubitRegister<N>` and a minimal entanglement witness.

```bash
python3 -m compiler.qpex run examples/basics/B11_qft_registers/main_qft_registers.qpex --seed 0
python3 -m compiler.qpex emit-qasm examples/basics/B11_qft_registers/main_qft_registers.qpex
```

Legacy source: `examples/16_quantum_observatory/qpu/portable_observatory_link.qpex`.
