// Classical notes for the Grover oracle target (corridor index 2 = Express).
// Harvested into main as Float `target` (ADR 0061).

package com.qpex.examples.city_route_search.operators

namespace City.Oracle {
    pub fn congestion_free_index() -> State<Float> {
        Float target = 2.0
        return target
    }
}
