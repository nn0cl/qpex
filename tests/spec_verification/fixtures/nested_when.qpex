package com.qpex.fixtures

public fun main() {
    state s0 = coin()
    state s1 = coin()
    state agree = when (s0) {
      0 -> when (s1) { 0 -> 1, else -> 0 },
      else -> when (s1) { 0 -> 0, else -> 1 },
    }
    measure agree
}
